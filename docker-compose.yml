version: '3.8'

services:
  peakprinting-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    container_name: peakprinting-react-app
    restart: unless-stopped