version: '3.8'

services:
  peakprinting-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "2323:3000"
    container_name: peakprinting-react-app
    restart: unless-stopped